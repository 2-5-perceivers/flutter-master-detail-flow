/// Describes the actual focus
enum Focus {
  /// Focus on the master
  master,

  /// Focus on details
  details,
}

/// Describes an app bar type
enum DetailsAppBarSize {
  /// Using a normal app bar
  small,

  /// Using a M3 medium collapsible app bar
  medium,

  /// Using a M3 medium collapsible app bar
  large,
}
