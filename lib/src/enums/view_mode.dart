/// An enum to determine the view mode of the MD flow.
enum MDViewMode {
  /// The master and details are displayed side by side.
  lateral,

  /// The master and details are displayed in a nested page view.
  page,
}
